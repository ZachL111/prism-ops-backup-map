# Review Journal

This journal records the domain cases that matter before widening the public API.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its automation focus without claiming live deployment or external usage.

## Cases

- `baseline`: `dry-run spread`, score 141, lane `ship`
- `stress`: `rename risk`, score 232, lane `ship`
- `edge`: `operator cost`, score 151, lane `ship`
- `recovery`: `idempotence`, score 167, lane `ship`
- `stale`: `dry-run spread`, score 181, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
